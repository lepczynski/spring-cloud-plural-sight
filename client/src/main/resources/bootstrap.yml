spring:
  application:
    name: cloud-client
  cloud:
#    Config First config bootstrapping
#    config:
#      uri: http://localhost:8888

#    Discovery First config bootstrapping
    discovery:
      enabled: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: false
server:
  port: 7777