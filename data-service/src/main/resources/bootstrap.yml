spring:
  application:
    name: service2
  cloud:
    config:
      discovery:
        enabled: true
        service-id: configuration-server
      fail-fast: true
      retry:
        max-attempts: 60

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

service:
  instance:
    name: service2b
